# Adapter Contract Cases

For every adapter:

1. `adapter.yaml` exists.
2. `README.md` exists.
3. Canonical skills remain under `skills/`.
4. Provider-specific claims are not marked verified without repository evidence.
5. The adapter does not contain a second copy of a canonical skill.
6. Installation guidance is explicitly host-dependent where current behavior is not tested.
