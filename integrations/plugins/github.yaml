version: 0.1.0
kind: plugin
id: github
description: Interact with authorized GitHub resources such as repositories, issues, and pull requests.
requires:
  connectors:
    - github-api
trust:
  authorization_required: true
  scope: host-defined
  destructive_actions: explicit
evidence:
  exposes_results: true
  supports_verification: task-dependent
