version: 0.1.0
kind: plugin
id: http
description: Make authorized HTTP requests for testing or retrieval.
requires:
  connectors:
    - http-client
trust:
  authorization_required: true
  scope: host-defined
  destructive_actions: explicit
evidence:
  exposes_results: true
  supports_verification: task-dependent
