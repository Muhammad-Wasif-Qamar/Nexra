version: 0.1.1
kind: plugin
id: container
description: Build or execute isolated containers where the host provides that capability.
trust:
  authorization_required: true
  scope: host-defined
  destructive_actions: explicit
evidence:
  exposes_results: true
  supports_verification: task-dependent
