version: 0.1.0
kind: plugin
id: mcp
description: Discover and invoke authorized MCP tools/resources exposed to the agent.
requires:
  connectors:
    - mcp-server
trust:
  authorization_required: true
  scope: host-defined
  destructive_actions: explicit
evidence:
  exposes_results: true
  supports_verification: task-dependent
