version: 0.5.0
kind: plugin
id: shell
description: Execute commands in an authorized shell environment.
requires:
  connectors:
    - shell-runtime
trust:
  authorization_required: true
  scope: host-defined
  destructive_actions: explicit
evidence:
  exposes_results: true
  supports_verification: task-dependent
