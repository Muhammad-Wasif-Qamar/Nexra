version: 0.1.1
kind: plugin
id: filesystem
description: Read and write files within the authorized workspace.
requires:
  connectors:
    - local-filesystem
trust:
  authorization_required: true
  scope: host-defined
  destructive_actions: explicit
evidence:
  exposes_results: true
  supports_verification: task-dependent
