version: 0.1.1
kind: connector
id: mcp-server
description: MCP server/resource transport.
authorization:
  required: true
  scope: host-defined
lifecycle:
  persistent: host-dependent
  cleanup_required: true
