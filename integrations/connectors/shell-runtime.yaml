version: 0.5.0
kind: connector
id: shell-runtime
description: Authorized shell/runtime execution resource.
authorization:
  required: true
  scope: host-defined
lifecycle:
  persistent: host-dependent
  cleanup_required: true
