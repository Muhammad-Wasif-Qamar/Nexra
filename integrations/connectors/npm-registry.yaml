version: 0.1.0
kind: connector
id: npm-registry
description: Package metadata and registry resource access.
authorization:
  required: true
  scope: host-defined
lifecycle:
  persistent: host-dependent
  cleanup_required: true
