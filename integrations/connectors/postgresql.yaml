version: 0.5.0
kind: connector
id: postgresql
description: Authorized PostgreSQL resource connection.
authorization:
  required: true
  scope: host-defined
lifecycle:
  persistent: host-dependent
  cleanup_required: true
