version: 0.5.0
kind: connector
id: http-client
description: Authorized HTTP transport resource.
authorization:
  required: true
  scope: host-defined
lifecycle:
  persistent: host-dependent
  cleanup_required: true
