version: 0.1.1
kind: connector
id: local-filesystem
description: Workspace file resource access.
authorization:
  required: true
  scope: host-defined
lifecycle:
  persistent: host-dependent
  cleanup_required: true
