version: 0.1.1
kind: connector
id: browser-session
description: Stateful browser/page session resource.
authorization:
  required: true
  scope: host-defined
lifecycle:
  persistent: host-dependent
  cleanup_required: true
